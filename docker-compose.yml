services:
  app:
    build: .
    restart: unless-stopped
    environment:
      PORT: 3000
    ports:
      - "127.0.0.1:${TRAVELS_APP_HOST_PORT:-3006}:3000"
    networks:
      - apps-net

networks:
  apps-net:
    external: true
